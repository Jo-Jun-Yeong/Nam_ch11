
public class map_prac {
/*
 * 	# map
	- 순서x, 중복(key-x, value-o)
		ex) 아이디-비밀번호
		
 * >메서드
	 * -추가-
	 * Object put(key , value) : 성공시 Object 반환 
	 * 
	 * -삭제-
	 * void clear() : 컬렉션의 모든 값을 삭제한다.
	 * Object remove(Object Key) : 해당 Key값을 가지고있는 Value값을 삭제한다. 
	 * 
	 * -검색-
	 * boolean containsKey(Object vlaue) : value의 값과 매칭된 Key값이 있는지 검색한다.
	 * boolean containsValue(Object key) : key값과 매칭된 Value값이 있는지 검색한다.
	 * boolean isEmpty()
	 * -반환-
	 * Object get(Object Key) : 해당 key값을 반환한다.
	 * int Size() 
	 * Collection valeu() : 저장된 모든 값을 collection에 저장 후 반환한다.
 * 
 *  
 * */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
